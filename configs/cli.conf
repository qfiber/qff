# configs/cli.conf
# Configuration for the QFF Command-Line Interface

[cli]
# The base URL of the qff-engine API server
api_base = http://localhost:8080

# Timeout for API requests made by the CLI
timeout = 30s

# Default number of log lines to fetch with the 'logs' command
default_log_lines = 50
